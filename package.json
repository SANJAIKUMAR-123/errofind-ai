{
  "name": "errofind-ai",
  "version": "1.0.0",
  "description": "AI-powered error explainer (Gemini API) that analyzes real code instead of matching a static database.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
